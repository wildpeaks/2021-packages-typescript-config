/* eslint-env browser */
import * as raw from './styles.scss';
const css = raw as {
	myclass: string;
};

export function mymodule(container: HTMLElement): void {
	container.innerText = `SCSS .myclass is a ${typeof css.myclass}`;
}
