/* eslint-env browser */
import * as raw from './styles.scss';
const css = raw as {
	myclass: string;
};

export function mymodule(container: HTMLElement): void {
	container.setAttribute('class', css.myclass);
	container.innerText = 'SCSS Hello World';
}
