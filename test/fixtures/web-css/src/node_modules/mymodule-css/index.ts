/* eslint-env browser */
import * as raw from './styles.css';
const css = raw as {
	myclass: string;
};

export function mymodule(container: HTMLElement): void {
	container.setAttribute('class', css.myclass);
	container.innerText = 'CSS Hello World';
}
