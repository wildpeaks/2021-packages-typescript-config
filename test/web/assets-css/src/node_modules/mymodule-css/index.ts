/* eslint-env browser */
import * as raw from './styles.css';
const css = raw as {
	myclass: string;
};

export function mymodule(container: HTMLElement): void {
	container.innerText = `[ASSETS CSS] .myclass is a ${typeof css.myclass}`;
}
