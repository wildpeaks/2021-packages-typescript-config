/* eslint-env browser */
import * as asset from './example1.jpg';

export function mymodule(container: HTMLElement): void {
	const img = document.createElement('img');
	img.setAttribute('src', String(asset));
	container.appendChild(img);
}
